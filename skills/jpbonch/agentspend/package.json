{
  "name": "agentspend-monorepo",
  "version": "0.1.2",
  "private": true,
  "workspaces": [
    "sdk",
    "cli"
  ],
  "scripts": {
    "build": "npm run build -w @agentspend/sdk && npm run build -w agentspend",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.9.3"
  }
}
